In order to prepare your environment, you can run the following command

```
python3 -m venv env
source env/bin/activate
pip install -r requirements.txt
```

However, the requirements aren't anything special. The only one you may not have
already is "plotly." 